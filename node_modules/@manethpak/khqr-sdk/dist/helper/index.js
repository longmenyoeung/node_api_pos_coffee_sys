"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/qr/helper/index.ts
var helper_exports = {};
__export(helper_exports, {
  ERROR_CODES: () => ERROR_CODES,
  KHQRError: () => KHQRError,
  calculateCRC16: () => calculateCRC16,
  createAdditionalData: () => createAdditionalData,
  createCRCTag: () => createCRCTag,
  createCountryCode: () => createCountryCode,
  createLanguageTemplate: () => createLanguageTemplate,
  createMerchantAccountInformation: () => createMerchantAccountInformation,
  createMerchantCategoryCode: () => createMerchantCategoryCode,
  createMerchantCity: () => createMerchantCity,
  createMerchantName: () => createMerchantName,
  createPayloadFormatIndicator: () => createPayloadFormatIndicator,
  createPointOfInitiationMethod: () => createPointOfInitiationMethod,
  createTimestamp: () => createTimestamp,
  createTransactionAmount: () => createTransactionAmount,
  createTransactionCurrency: () => createTransactionCurrency,
  createUnionpayMerchantAccount: () => createUnionpayMerchantAccount,
  error: () => error,
  failed: () => failed,
  formatTag: () => formatTag,
  parseTag: () => parseTag,
  success: () => success,
  tag: () => tag,
  validateCRC: () => validateCRC,
  validators: () => validators,
  wrap: () => wrap
});
module.exports = __toCommonJS(helper_exports);

// src/qr/helper/crc.ts
var CRC_TABLE = [
  0,
  4129,
  8258,
  12387,
  16516,
  20645,
  24774,
  28903,
  33032,
  37161,
  41290,
  45419,
  49548,
  53677,
  57806,
  61935,
  4657,
  528,
  12915,
  8786,
  21173,
  17044,
  29431,
  25302,
  37689,
  33560,
  45947,
  41818,
  54205,
  50076,
  62463,
  58334,
  9314,
  13379,
  1056,
  5121,
  25830,
  29895,
  17572,
  21637,
  42346,
  46411,
  34088,
  38153,
  58862,
  62927,
  50604,
  54669,
  13907,
  9842,
  5649,
  1584,
  30423,
  26358,
  22165,
  18100,
  46939,
  42874,
  38681,
  34616,
  63455,
  59390,
  55197,
  51132,
  18628,
  22757,
  26758,
  30887,
  2112,
  6241,
  10242,
  14371,
  51660,
  55789,
  59790,
  63919,
  35144,
  39273,
  43274,
  47403,
  23285,
  19156,
  31415,
  27286,
  6769,
  2640,
  14899,
  10770,
  56317,
  52188,
  64447,
  60318,
  39801,
  35672,
  47931,
  43802,
  27814,
  31879,
  19684,
  23749,
  11298,
  15363,
  3168,
  7233,
  60846,
  64911,
  52716,
  56781,
  44330,
  48395,
  36200,
  40265,
  32407,
  28342,
  24277,
  20212,
  15891,
  11826,
  7761,
  3696,
  65439,
  61374,
  57309,
  53244,
  48923,
  44858,
  40793,
  36728,
  37256,
  33193,
  45514,
  41451,
  53516,
  49453,
  61774,
  57711,
  4224,
  161,
  12482,
  8419,
  20484,
  16421,
  28742,
  24679,
  33721,
  37784,
  41979,
  46042,
  49981,
  54044,
  58239,
  62302,
  689,
  4752,
  8947,
  13010,
  16949,
  21012,
  25207,
  29270,
  46570,
  42443,
  38312,
  34185,
  62830,
  58703,
  54572,
  50445,
  13538,
  9411,
  5280,
  1153,
  29798,
  25671,
  21540,
  17413,
  42971,
  47098,
  34713,
  38840,
  59231,
  63358,
  50973,
  55100,
  9939,
  14066,
  1681,
  5808,
  26199,
  30326,
  17941,
  22068,
  55628,
  51565,
  63758,
  59695,
  39368,
  35305,
  47498,
  43435,
  22596,
  18533,
  30726,
  26663,
  6336,
  2273,
  14466,
  10403,
  52093,
  56156,
  60223,
  64286,
  35833,
  39896,
  43963,
  48026,
  19061,
  23124,
  27191,
  31254,
  2801,
  6864,
  10931,
  14994,
  64814,
  60687,
  56684,
  52557,
  48554,
  44427,
  40424,
  36297,
  31782,
  27655,
  23652,
  19525,
  15522,
  11395,
  7392,
  3265,
  61215,
  65342,
  53085,
  57212,
  44955,
  49082,
  36825,
  40952,
  28183,
  32310,
  20053,
  24180,
  11923,
  16050,
  3793,
  7920
];
function calculateCRC16(input) {
  let crc = 65535;
  const buffer = Buffer.from(input, "utf8");
  for (let i = 0; i < buffer.length; i++) {
    const c = buffer[i];
    const j = (c ^ crc >> 8) & 255;
    crc = CRC_TABLE[j] ^ crc << 8;
  }
  const finalCheckSum = (crc ^ 0) & 65535;
  return finalCheckSum.toString(16).toUpperCase().padStart(4, "0");
}
function validateCRC(qrString) {
  if (qrString.length < 4) return false;
  const crcFromQR = qrString.slice(-4).toUpperCase();
  const dataWithoutCRC = qrString.slice(0, -4);
  const calculatedCRC = calculateCRC16(dataWithoutCRC);
  return crcFromQR === calculatedCRC;
}

// src/qr/helper/errors.ts
var ERROR_CODES = {
  INVALID_QR: "INVALID_QR",
  INVALID_AMOUNT: "INVALID_AMOUNT",
  INVALID_ACCOUNT: "INVALID_ACCOUNT",
  REQUIRED_FIELD: "REQUIRED_FIELD",
  INVALID_FORMAT: "INVALID_FORMAT",
  CRC_INVALID: "CRC_INVALID"
};
var KHQRError = class extends Error {
  constructor(code, message, details) {
    super(message);
    this.name = "KHQRError";
    this.code = code;
    this.details = details;
  }
  toJSON() {
    return {
      name: this.name,
      code: this.code,
      message: this.message,
      details: this.details ?? null
    };
  }
};
var error = {
  invalidQR: (message = "Invalid QR code format", details) => new KHQRError(ERROR_CODES.INVALID_QR, message, details),
  invalidAmount: (message = "Invalid amount for currency", details) => new KHQRError(ERROR_CODES.INVALID_AMOUNT, message, details),
  invalidAccount: (message = "Invalid account information", details) => new KHQRError(ERROR_CODES.INVALID_ACCOUNT, message, details),
  requiredField: (field, details) => new KHQRError(
    ERROR_CODES.REQUIRED_FIELD,
    `Field '${field}' is required`,
    details ?? { field }
  ),
  invalidFormat: (fields, details) => new KHQRError(
    ERROR_CODES.INVALID_FORMAT,
    `Invalid or Missing fields '${fields}'`,
    details
  ),
  invalidCRC: (details) => new KHQRError(ERROR_CODES.CRC_INVALID, "CRC checksum is invalid", details),
  invalidQRString: (details) => new KHQRError(
    ERROR_CODES.INVALID_QR,
    "QR string must be a non-empty string",
    details
  )
};

// src/qr/helper/result.ts
var success = (value) => ({
  result: value,
  error: null
});
function failed(errorOrMessage, details, code = ERROR_CODES.INVALID_FORMAT) {
  const err = errorOrMessage instanceof KHQRError ? errorOrMessage : new KHQRError(code, errorOrMessage, details);
  return {
    result: null,
    error: err
  };
}
var wrap = async (fn) => {
  try {
    const value = await fn();
    return success(value);
  } catch (err) {
    if (err instanceof KHQRError) {
      return failed(err);
    }
    const message = err instanceof Error ? err.message : "Unknown error";
    return failed(message);
  }
};

// src/qr/constants/emv.ts
var EMV_TAGS = {
  // Standard EMV tags
  PAYLOAD_FORMAT_INDICATOR: "00",
  POINT_OF_INITIATION_METHOD: "01",
  MERCHANT_ACCOUNT_INFORMATION_INDIVIDUAL: "29",
  MERCHANT_ACCOUNT_INFORMATION_MERCHANT: "30",
  MERCHANT_CATEGORY_CODE: "52",
  TRANSACTION_CURRENCY: "53",
  TRANSACTION_AMOUNT: "54",
  COUNTRY_CODE: "58",
  MERCHANT_NAME: "59",
  MERCHANT_CITY: "60",
  ADDITIONAL_DATA_TAG: "62",
  CRC: "63",
  MERCHANT_INFORMATION_LANGUAGE_TEMPLATE: "64",
  TIMESTAMP_TAG: "99",
  // UnionPay specific
  UNIONPAY_MERCHANT_ACCOUNT: "15",
  // Sub-tags for merchant account information
  BAKONG_ACCOUNT_IDENTIFIER: "00",
  MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID: "01",
  INDIVIDUAL_ACCOUNT_INFORMATION: "01",
  MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK: "02",
  // Additional data sub-tags
  BILLNUMBER_TAG: "01",
  ADDITIONAL_DATA_FIELD_MOBILE_NUMBER: "02",
  STORELABEL_TAG: "03",
  TERMINAL_TAG: "07",
  PURPOSE_OF_TRANSACTION: "08",
  // Timestamp sub-tags
  CREATION_TIMESTAMP: "00",
  EXPIRATION_TIMESTAMP: "01",
  // Language template sub-tags
  LANGUAGE_PREFERENCE: "00",
  MERCHANT_NAME_ALTERNATE_LANGUAGE: "01",
  MERCHANT_CITY_ALTERNATE_LANGUAGE: "02"
};
var EMV_DEFAULTS = {
  PAYLOAD_FORMAT_INDICATOR: "01",
  MERCHANT_CATEGORY_CODE: "5999",
  COUNTRY_CODE: "KH",
  MERCHANT_CITY: "Phnom Penh",
  CRC_LENGTH: "04"
};
var QR_TYPE_INDICATORS = {
  STATIC_QR: "11",
  DYNAMIC_QR: "12"
};
var CURRENCY_CODES = {
  KHR: "116",
  USD: "840"
};

// src/qr/helper/tags.ts
function formatTag(tag2, value) {
  const length = value.length.toString().padStart(2, "0");
  return `${tag2}${length}${value}`;
}
function parseTag(data, offset = 0) {
  if (data.length < offset + 4) {
    throw new Error("Invalid tag format: insufficient length");
  }
  const tag2 = data.substring(offset, offset + 2);
  const length = parseInt(data.substring(offset + 2, offset + 4), 10);
  const value = data.substring(offset + 4, offset + 4 + length);
  const remainingString = data.substring(offset + 4 + length);
  return {
    tag: tag2,
    length,
    value,
    remainingString
  };
}
function createPayloadFormatIndicator() {
  return formatTag(
    EMV_TAGS.PAYLOAD_FORMAT_INDICATOR,
    EMV_DEFAULTS.PAYLOAD_FORMAT_INDICATOR
  );
}
function createPointOfInitiationMethod(isStatic) {
  const value = isStatic ? QR_TYPE_INDICATORS.STATIC_QR : QR_TYPE_INDICATORS.DYNAMIC_QR;
  return formatTag(EMV_TAGS.POINT_OF_INITIATION_METHOD, value);
}
function createMerchantAccountInformation(type, info) {
  const tag2 = type === "individual" ? EMV_TAGS.MERCHANT_ACCOUNT_INFORMATION_INDIVIDUAL : EMV_TAGS.MERCHANT_ACCOUNT_INFORMATION_MERCHANT;
  let subTags = "";
  subTags += formatTag(EMV_TAGS.BAKONG_ACCOUNT_IDENTIFIER, info.bakongAccountID);
  if (type === "merchant") {
    const merchantInfo = info;
    if (merchantInfo.merchantID) {
      subTags += formatTag(
        EMV_TAGS.MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID,
        merchantInfo.merchantID
      );
    }
    if (merchantInfo.acquiringBank) {
      subTags += formatTag(
        EMV_TAGS.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK,
        merchantInfo.acquiringBank
      );
    }
  } else {
    if (info.accountInformation) {
      subTags += formatTag(
        EMV_TAGS.INDIVIDUAL_ACCOUNT_INFORMATION,
        info.accountInformation
      );
    }
    if (info.acquiringBank) {
      subTags += formatTag(
        EMV_TAGS.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK,
        info.acquiringBank
      );
    }
  }
  return formatTag(tag2, subTags);
}
function createUnionpayMerchantAccount(accountInfo) {
  return formatTag(EMV_TAGS.UNIONPAY_MERCHANT_ACCOUNT, accountInfo);
}
function createMerchantCategoryCode(categoryCode) {
  const code = categoryCode || EMV_DEFAULTS.MERCHANT_CATEGORY_CODE;
  return formatTag(EMV_TAGS.MERCHANT_CATEGORY_CODE, code);
}
function createTransactionCurrency(currency = "KHR") {
  const currencyCode = currency === "KHR" ? CURRENCY_CODES.KHR : CURRENCY_CODES.USD;
  return formatTag(EMV_TAGS.TRANSACTION_CURRENCY, currencyCode);
}
function createTransactionAmount(amount, currency) {
  let formattedAmount;
  if (currency === "KHR") {
    formattedAmount = Math.round(amount).toString();
  } else {
    formattedAmount = amount.toFixed(2);
  }
  return formatTag(EMV_TAGS.TRANSACTION_AMOUNT, formattedAmount);
}
function createCountryCode() {
  return formatTag(EMV_TAGS.COUNTRY_CODE, EMV_DEFAULTS.COUNTRY_CODE);
}
function createMerchantName(name) {
  return formatTag(EMV_TAGS.MERCHANT_NAME, name);
}
function createMerchantCity(city) {
  const merchantCity = city || EMV_DEFAULTS.MERCHANT_CITY;
  return formatTag(EMV_TAGS.MERCHANT_CITY, merchantCity);
}
function createAdditionalData(additionalInfo) {
  let subTags = "";
  if (additionalInfo.billNumber) {
    subTags += formatTag(EMV_TAGS.BILLNUMBER_TAG, additionalInfo.billNumber);
  }
  if (additionalInfo.mobileNumber) {
    subTags += formatTag(
      EMV_TAGS.ADDITIONAL_DATA_FIELD_MOBILE_NUMBER,
      additionalInfo.mobileNumber
    );
  }
  if (additionalInfo.storeLabel) {
    subTags += formatTag(EMV_TAGS.STORELABEL_TAG, additionalInfo.storeLabel);
  }
  if (additionalInfo.terminalLabel) {
    subTags += formatTag(EMV_TAGS.TERMINAL_TAG, additionalInfo.terminalLabel);
  }
  if (additionalInfo.purposeOfTransaction) {
    subTags += formatTag(
      EMV_TAGS.PURPOSE_OF_TRANSACTION,
      additionalInfo.purposeOfTransaction
    );
  }
  return subTags ? formatTag(EMV_TAGS.ADDITIONAL_DATA_TAG, subTags) : "";
}
function createLanguageTemplate(languageInfo) {
  let subTags = "";
  if (languageInfo.languagePreference) {
    subTags += formatTag(
      EMV_TAGS.LANGUAGE_PREFERENCE,
      languageInfo.languagePreference
    );
  }
  if (languageInfo.merchantNameAlternateLanguage) {
    subTags += formatTag(
      EMV_TAGS.MERCHANT_NAME_ALTERNATE_LANGUAGE,
      languageInfo.merchantNameAlternateLanguage
    );
  }
  if (languageInfo.merchantCityAlternateLanguage) {
    subTags += formatTag(
      EMV_TAGS.MERCHANT_CITY_ALTERNATE_LANGUAGE,
      languageInfo.merchantCityAlternateLanguage
    );
  }
  return subTags ? formatTag(EMV_TAGS.MERCHANT_INFORMATION_LANGUAGE_TEMPLATE, subTags) : "";
}
function createTimestamp(creationTimestamp, expirationTimestamp) {
  let subTags = "";
  subTags += formatTag(
    EMV_TAGS.CREATION_TIMESTAMP,
    creationTimestamp.toString()
  );
  subTags += formatTag(
    EMV_TAGS.EXPIRATION_TIMESTAMP,
    expirationTimestamp.toString()
  );
  return formatTag(EMV_TAGS.TIMESTAMP_TAG, subTags);
}
function createCRCTag(crcValue) {
  return formatTag(EMV_TAGS.CRC, crcValue);
}
var tag = {
  payloadFormatIndicator: createPayloadFormatIndicator,
  pointOfInitiationMethod: createPointOfInitiationMethod,
  merchantAccountInformation: createMerchantAccountInformation,
  unionpayMerchantAccount: createUnionpayMerchantAccount,
  merchantCategoryCode: createMerchantCategoryCode,
  transactionCurrency: createTransactionCurrency,
  transactionAmount: createTransactionAmount,
  countryCode: createCountryCode,
  merchantName: createMerchantName,
  merchantCity: createMerchantCity,
  additionalData: createAdditionalData,
  languageTemplate: createLanguageTemplate,
  timestamp: createTimestamp,
  crc: createCRCTag
};

// src/qr/helper/validator.ts
var validators = {
  /**
   * Validate required string field
   */
  required: (value, fieldName) => {
    if (!value || value.trim() === "") {
      return `${fieldName} is required`;
    }
    return null;
  },
  /**
   * Validate string length
   */
  maxLength: (value, maxLen, fieldName) => {
    if (value && value.length > maxLen) {
      return `${fieldName} must not exceed ${maxLen} characters`;
    }
    return null;
  },
  /**
   * Validate amount based on currency
   */
  amount: (amount, currency) => {
    if (amount === void 0 || amount === 0) return null;
    if (amount < 0) return "Amount cannot be negative";
    if (currency === "KHR") {
      if (!Number.isInteger(amount)) {
        return "KHR amount must be a whole number";
      }
    } else if (currency === "USD") {
      const decimalPlaces = (amount.toString().split(".")[1] || "").length;
      if (decimalPlaces > 2) {
        return "USD amount cannot have more than 2 decimal places";
      }
    }
    return null;
  },
  /**
   * Validate Bakong account ID format
   */
  bakongAccountID: (accountId) => {
    if (!accountId) return "Bakong account ID is required";
    if (accountId.length > 32) return "Bakong account ID too long";
    if (!/^[a-zA-Z0-9._@-]+$/.test(accountId)) {
      return "Bakong account ID contains invalid characters";
    }
    return null;
  },
  /**
   * Validate merchant category code
   */
  merchantCategoryCode: (code) => {
    if (code && !/^\d{4}$/.test(code)) {
      return "Merchant category code must be 4 digits";
    }
    return null;
  },
  /**
   * Validate currency
   */
  currency: (currency) => {
    if (!["KHR", "USD"].includes(currency)) {
      return "Currency must be KHR or USD";
    }
    return null;
  },
  /**
   * Validate expiration timestamp for dynamic QR
   */
  expirationTimestamp: (timestamp, qrType) => {
    if (qrType === "dynamic") {
      if (!timestamp) return "Expiration timestamp is required for dynamic QR";
      if (timestamp <= Date.now())
        return "Expiration timestamp must be in the future";
    }
    return null;
  },
  /**
   * Comprehensive validation for IndividualInfo
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  validateIndividualInfo: (info) => {
    const errors = [];
    const requiredError = validators.required(
      info.bakongAccountID,
      "Bakong Account ID"
    );
    if (requiredError) errors.push(requiredError);
    const nameError = validators.required(info.merchantName, "Merchant Name");
    if (nameError) errors.push(nameError);
    const cityError = validators.required(info.merchantCity, "Merchant City");
    if (cityError) errors.push(cityError);
    const accountError = validators.bakongAccountID(info.bakongAccountID);
    if (accountError) errors.push(accountError);
    const lengthErrors = [
      validators.maxLength(info.merchantName, 25, "Merchant Name"),
      validators.maxLength(info.merchantCity, 15, "Merchant City"),
      validators.maxLength(info.billNumber, 25, "Bill Number"),
      validators.maxLength(info.mobileNumber, 25, "Mobile Number"),
      validators.maxLength(info.storeLabel, 25, "Store Label"),
      validators.maxLength(info.terminalLabel, 25, "Terminal Label")
    ].filter((error2) => error2 !== null);
    errors.push(...lengthErrors);
    const currency = info.currency || "KHR";
    const currencyError = validators.currency(currency);
    if (currencyError) errors.push(currencyError);
    const amountError = validators.amount(info.amount, currency);
    if (amountError) errors.push(amountError);
    const categoryError = validators.merchantCategoryCode(
      info.merchantCategoryCode
    );
    if (categoryError) errors.push(categoryError);
    const qrType = info.amount && info.amount > 0 ? "dynamic" : "static";
    const timestampError = validators.expirationTimestamp(
      info.expirationTimestamp,
      qrType
    );
    if (timestampError) errors.push(timestampError);
    return {
      isValid: errors.length === 0,
      errors
    };
  },
  /**
   * Comprehensive validation for MerchantInfo
   * Extends IndividualInfo validation
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  validateMerchantInfo: (info) => {
    const baseValidation = validators.validateIndividualInfo(info);
    const errors = [...baseValidation.errors];
    const merchantIdError = validators.required(info.merchantID, "Merchant ID");
    if (merchantIdError) errors.push(merchantIdError);
    const acquiringBankError = validators.required(
      info.acquiringBank,
      "Acquiring Bank"
    );
    if (acquiringBankError) errors.push(acquiringBankError);
    const lengthErrors = [
      validators.maxLength(info.merchantID, 32, "Merchant ID"),
      validators.maxLength(info.acquiringBank, 32, "Acquiring Bank")
    ].filter((error2) => error2 !== null);
    errors.push(...lengthErrors);
    return {
      isValid: errors.length === 0,
      errors
    };
  },
  /**
   * Basic QR string format validation
   */
  validateQRString: (qrString) => {
    const errors = [];
    if (!qrString) {
      errors.push("QR string is required");
      return { isValid: false, errors };
    }
    if (qrString.length < 12) {
      errors.push("QR string is too short");
    }
    if (!/6304[A-Fa-f0-9]{4}$/.test(qrString)) {
      errors.push("Invalid CRC format");
    }
    return {
      isValid: errors.length === 0,
      errors
    };
  },
  /**
   * Check if info is MerchantInfo. Can be use as type guard.
   *
   * @param info - any object
   * @returns boolean
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  isMerchantInfo: (info) => {
    return info && typeof info === "object" && "merchantID" in info && typeof info.merchantID === "string";
  },
  /**
   * type guard for IndividualInfo. Can be use as type guard.
   *
   * @param info - any object
   * @returns boolean
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  isIndividualInfo: (info) => {
    return info && typeof info === "object" && "bakongAccountID" in info && typeof info.bakongAccountID === "string";
  },
  /**
   * Check if string is Static QR
   */
  isStaticQR: (qrString) => {
    if (!qrString) return false;
    return /^000201010211/.test(qrString);
  },
  /**
   * Check if string is Dynamic QR
   */
  isDynamicQR: (qrString) => {
    if (!qrString) return false;
    return /^000201010212/.test(qrString);
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ERROR_CODES,
  KHQRError,
  calculateCRC16,
  createAdditionalData,
  createCRCTag,
  createCountryCode,
  createLanguageTemplate,
  createMerchantAccountInformation,
  createMerchantCategoryCode,
  createMerchantCity,
  createMerchantName,
  createPayloadFormatIndicator,
  createPointOfInitiationMethod,
  createTimestamp,
  createTransactionAmount,
  createTransactionCurrency,
  createUnionpayMerchantAccount,
  error,
  failed,
  formatTag,
  parseTag,
  success,
  tag,
  validateCRC,
  validators,
  wrap
});
